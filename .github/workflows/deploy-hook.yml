name: Manual Redeploy to Vercel (No Push)

on:
  workflow_dispatch: {}

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Vercel hook
        run: curl -X POST "${{ secrets.VERCEL_DEPLOY_HOOK_URL }}"
